{
    "version": 2,
    "buildCommand": "cd apps/web && pnpm config set fetch-retries 5 && pnpm config set fetch-timeout 30000 && pnpm run build",
    "outputDirectory": "apps/web/.next",
    "installCommand": "pnpm config set fetch-retries 5 && pnpm config set fetch-timeout 30000 && pnpm install --no-frozen-lockfile",
    "framework": "nextjs"
}